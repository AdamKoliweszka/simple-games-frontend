import { ColorsGenderPipe } from './colors-gender.pipe';

describe('ColorsGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorsGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
